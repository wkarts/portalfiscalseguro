# Correção da publicação inválida

Esta branch remove o workflow temporário quebrado que foi incorporado pela Pull Request #1.

## Estado confirmado

- a PR #1 não publicou o código-fonte da aplicação;
- somente `.github/workflows/bootstrap-source.yml` foi alterado;
- o repositório ainda não contém a aplicação Tauri/Rust/Vue;
- não há build ou release válida da aplicação neste repositório.

## Próxima publicação válida

A publicação válida deverá mostrar diretamente, na lista de arquivos da Pull Request, o código-fonte completo da aplicação e seus workflows definitivos. Não será aceito outro workflow intermediário como substituto do código-fonte.
